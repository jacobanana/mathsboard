node_modules
dist
*.tsbuildinfo
# Playwright output
test-results
playwright-report
# Deployment secrets (copy .env.example -> .env and fill in)
.env
.env.local
.env.*.local
# Private keys / certificates — never commit
*.pem
*.key
